export const SelectStyles = {
  control: (provided) => ({
    ...provided,
    height: "40px",
  }),
  valueContainer: (provided) => ({
    ...provided,
    height: "40px",
  }),
  indicatorsContainer: (provided) => ({
    ...provided,
    height: "40px",
  }),
};
